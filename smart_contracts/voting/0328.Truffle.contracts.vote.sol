pragma solidity 0.4.20;

contract vote_sytem(){
  string private name;
  uint private age;

  function setName(string newName) returns (string){
    return name = newName;
  }
  function getName() returns (string){
    return name;
  }
  function getAge() returns (uint){
    return name;
  }
}
}
